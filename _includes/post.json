{
    "category":"{{item.categories}}",
    "title":"{{item.title|replace:'"','\"'}}",
    "tags": {
        {% for tag in item.tags %}"tag":"{{tag}}"{% if forloop.last != true %},{% else %}{% endif %}{% endfor %}
    },
    "icon24":"{{item.icon-24}}",
    "icon18":"{{item.icon-18}}",
    "icon12":"{{item.icon-12}}"
}
