{
    "title":"{{item.title|replace:'"','\"'}}",
    "tags": [
        {% for tag in item.tags %}"{{tag}}"{% unless forloop.last %},{% endunless %}{% endfor %}
    ],
    "icon":"{{item.title || downcase || replace:' ','-'}}"
}
